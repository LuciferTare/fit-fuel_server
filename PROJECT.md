# Fit & Fuel — Backend Project Documentation

## Overview

Fit & Fuel is a Django REST Framework backend that powers a Flutter fitness-management mobile application. The backend manages a multi-tenant gym ecosystem with four distinct user roles — Platform Admin, Gym Owner, Trainer, and Member — each with strictly scoped permissions and API surfaces.

The system migrates the Flutter app from local SQLite storage to a centralized production backend backed by MySQL.

---

## Tech Stack

| Layer              | Technology                            | Version                 |
| ------------------ | ------------------------------------- | ----------------------- |
| Framework          | Django                                | 5.2.4                   |
| API Layer          | Django REST Framework                 | 3.16.0                  |
| Database           | MySQL                                 | via `mysqlclient` 2.2.7 |
| Authentication     | JWT (`djangorestframework_simplejwt`) | 5.5.0                   |
| Schema / Docs      | `drf-spectacular` (Swagger UI)        | 0.28.0                  |
| CORS               | `django-cors-headers`                 | 4.7.0                   |
| Filtering          | `django-filter`                       | 25.1                    |
| GeoIP              | `geoip2` + GeoLite2 City database     | 5.1.0                   |
| IP Detection       | `django-ipware`                       | 7.0.1                   |
| User-Agent Parsing | `user-agents`                         | 2.2.0                   |
| Image Handling     | `Pillow`                              | 11.3.0                  |
| Config             | `python-dotenv`                       | 1.1.1                   |
| Language           | Python 3.x                            | —                       |

---

## Project Structure

```
fit&fuel_server/
├── fit_&_fuel/                  # Django project config package
│   ├── settings.py              # All settings (JWT, DB, email, logging)
│   ├── urls.py                  # Root URL routing
│   ├── wsgi.py                  # WSGI entry point
│   └── asgi.py                  # ASGI entry point
│
├── accounts/                    # User & auth app
│   ├── models.py                # CustomUser, Membership, Payment models + enums
│   ├── managers.py              # UserManager, ActiveUserManager
│   ├── serializers.py           # All serializers (auth, profile, CRUD, membership, payment, Phase-3 payment)
│   ├── validators.py            # StrongPasswordValidator
│   ├── views.py                 # Auth views (login, logout, me, change-password)
│   ├── user_views.py            # CRUD viewsets (GymOwner, Trainer, Member, Membership, Payment, MemberPaymentView)
│   ├── admin.py                 # Django Admin for CustomUser, Membership, Payment
│   ├── urls.py                  # /auth/ endpoints
│   ├── urls_users.py            # /users/ endpoints (Admin-managed users)
│   ├── urls_memberships.py      # /memberships/ and /payments/ endpoints (Phase-2)
│   ├── urls_member_payments.py  # /api/payments/ endpoint (Phase-3 member-centric payment)
│   ├── urls_trainer.py          # /trainer/ endpoints (Trainer panel)
│   ├── urls_member.py           # /member/ endpoints (Member panel)
│   ├── tests.py                 # Full test suite (auth, admin, trainer, member)
│   └── management/
│       └── commands/
│           └── create_admin.py  # CLI to bootstrap the first Platform Admin
│
├── attendance/                  # Attendance tracking app (Phase 3)
│   ├── models.py                # Attendance model with GPS check-in/out fields
│   ├── serializers.py           # AttendanceSerializer, CheckInSerializer, CheckOutSerializer
│   ├── views.py                 # CheckInView, CheckOutView, AttendanceListView
│   ├── urls.py                  # /api/attendance/ endpoints
│   ├── admin.py                 # Django Admin for Attendance
│   └── migrations/              # Auto-generated by makemigrations
│
├── reports/                     # Reporting endpoints app (Phase 3)
│   ├── views.py                 # InactiveMembersView, TrainerWorkloadView, MembershipExpiryView
│   └── urls.py                  # /api/reports/ endpoints
│
├── backup/                      # Offline sync / backup app (Phase 3)
│   ├── views.py                 # BackupUploadView (LWW sync), BackupDownloadView
│   └── urls.py                  # /api/backup/ endpoints
│
├── notifications/               # Notification stubs (Phase 3)
│   └── services.py              # send_sms(), send_whatsapp() — placeholders for Twilio
│
├── core/                        # Shared utilities app
│   ├── models.py                # BaseModel (abstract), SoftDeleteManager
│   ├── permissions.py           # IsAdmin, IsGymOwner, IsTrainer, IsMember, IsAdminOrGymOwner
│   ├── pagination.py            # CustomPagination (page/page_size, max 100)
│   ├── renderers.py             # ResponseRenderer — unified JSON envelope
│   ├── exceptions.py            # ConflictException (HTTP 409)
│   ├── views.py                 # BaseAPIView, BaseModelViewSet, ping, health_check, my_ip
│   └── urls.py                  # /api/ utility endpoints
│
├── static/geo_lite2/            # GeoLite2-City.mmdb (GeoIP database)
├── media/                       # Uploaded files (profile pictures)
├── logs/                        # Runtime log files (auto-created)
├── templates/                   # Django templates (swagger-ui.html, email templates)
├── requirements.txt             # Pinned Python dependencies
├── manage.py                    # Django management entrypoint
└── .env.example                 # Environment variable template
```

---

## Domain Model

### User Role Hierarchy

There is **one database table** (`users`) for all user types. Roles are distinguished by the `user_type` field. No separate Gym or Trainer models exist — gym identity is the UUID of the `GYM_OWNER` user.

```
ADMIN
  └── creates GYM_OWNERs
        └── creates TRAINERs (scoped to their gym)
        └── creates MEMBERs  (scoped to their gym)
              └── can be assigned to a TRAINER
```

### CustomUser Model

**Table:** `users`  
**Primary Key:** `uuid` (UUID4, not auto-int)  
**Auth identifier:** `phone_number` (`USERNAME_FIELD = "phone_number"`)

| Field               | Type           | Notes                                                             |
| ------------------- | -------------- | ----------------------------------------------------------------- |
| `uuid`              | UUIDField      | PK, auto-generated, not editable                                  |
| `phone_number`      | CharField(15)  | Unique — used as login username                                   |
| `first_name`        | CharField(150) | Optional                                                          |
| `last_name`         | CharField(150) | Optional                                                          |
| `date_of_birth`     | DateField      | Optional                                                          |
| `gender`            | CharField      | Choices: MALE / FEMALE / OTHER                                    |
| `profile_picture`   | ImageField     | Uploaded to `profile_pictures/`                                   |
| `user_type`         | CharField      | ADMIN / GYM_OWNER / TRAINER / MEMBER                              |
| `status`            | CharField      | ACTIVE / DISABLED / SUSPENDED / DELETED                           |
| `gym`               | FK → self      | Points to a `GYM_OWNER` user; null for Admin/GymOwner             |
| `trainer`           | FK → self      | Points to a `TRAINER` user; only set on MEMBERs                   |
| `trainer_limit`     | IntegerField   | Max trainers allowed for this Gym Owner (0 = unlimited)           |
| `membership_start`  | DateField      | Snapshot: start date of current/last membership (nullable)        |
| `membership_end`    | DateField      | Snapshot: end date of current/last membership (nullable, indexed) |
| `membership_status` | CharField      | Snapshot: ACTIVE / EXPIRED / null                                 |
| `membership_plan`   | CharField(100) | Snapshot: plan label from last payment (nullable)                 |
| `is_staff`          | BooleanField   | Django admin access flag                                          |
| `is_active`         | BooleanField   | Django auth flag                                                  |
| `created_by`        | FK → self      | Audit: who created this user                                      |
| `updated_by`        | FK → self      | Audit: who last updated this user                                 |
| `created_at`        | DateTimeField  | Auto set on create, indexed                                       |
| `updated_at`        | DateTimeField  | Auto set on update                                                |
| `is_deleted`        | BooleanField   | Soft-delete flag, indexed                                         |
| `deleted_at`        | DateTimeField  | Timestamp of soft deletion                                        |

**Indexes:** `db_index=True` on `user_type`, `status`, `created_at`, `is_deleted`, `membership_end`. Composite index `(gym, user_type)` for trainer/member queries.

**Membership snapshot fields** (`membership_start`, `membership_end`, `membership_status`, `membership_plan`) are denormalised copies updated whenever a payment is recorded via `POST /api/payments/`. They power the membership-expiry report without joining to the `memberships` table.

**Computed property:**

- `age` — calculated from `date_of_birth` relative to today's date.

**Methods:**

- `get_full_name()` — returns `"first_name last_name"` or falls back to `phone_number`
- `get_short_name()` — returns `first_name` or falls back to `phone_number`
- `soft_delete(deleted_by=None)` — sets `is_deleted=True`, `status=DELETED`, stamps `deleted_at`

**Relationship validation** (enforced in `clean()` / `_validate_relationships()`):

- ADMIN cannot have a gym or trainer assignment
- GYM_OWNER cannot have a gym or trainer assignment
- TRAINER cannot reference another trainer
- MEMBER's trainer must belong to the same gym as the member

### Membership Model

**Table:** `memberships`  
**Primary Key:** `uuid` (inherited from `BaseModel`)  
Tracks one membership term for a Member. A Member may accumulate multiple records over time (renewals). Only one `ACTIVE` membership with non-overlapping dates is enforced per Member.

| Field                       | Type               | Notes                                       |
| --------------------------- | ------------------ | ------------------------------------------- |
| `uuid`                      | UUIDField          | PK, auto-generated                          |
| `member`                    | FK → CustomUser    | Must be `user_type=MEMBER`                  |
| `start_date`                | DateField          | Inclusive start; must be ≤ `end_date`       |
| `end_date`                  | DateField          | Inclusive end                               |
| `plan`                      | CharField(100)     | Optional label, e.g. "Monthly", "Quarterly" |
| `amount_paid`               | DecimalField(10,2) | Total amount paid for this term             |
| `payment_mode`              | CharField          | CASH / ONLINE                               |
| `status`                    | CharField          | ACTIVE / EXPIRED (default: ACTIVE)          |
| `created_by` / `updated_by` | FK → User          | Audit (from BaseModel)                      |
| `created_at` / `updated_at` | DateTimeField      | Auto-managed                                |
| `is_deleted` / `deleted_at` | —                  | Soft-delete (from BaseModel)                |

### Payment Model

**Table:** `payments`  
**Primary Key:** `uuid` (inherited from `BaseModel`)  
Records an individual payment transaction against a Membership term. A membership may have multiple payments (e.g. installments).

| Field                       | Type               | Notes                                      |
| --------------------------- | ------------------ | ------------------------------------------ |
| `uuid`                      | UUIDField          | PK, auto-generated                         |
| `membership`                | FK → Membership    | The membership this payment covers         |
| `amount`                    | DecimalField(10,2) | Must be > 0                                |
| `mode`                      | CharField          | CASH / ONLINE                              |
| `paid_on`                   | DateTimeField      | Defaults to `timezone.now` if not provided |
| `created_by` / `updated_by` | FK → User          | Audit (from BaseModel)                     |
| `created_at` / `updated_at` | DateTimeField      | Auto-managed                               |
| `is_deleted` / `deleted_at` | —                  | Soft-delete (from BaseModel)               |

### Attendance Model

**Table:** `attendance`  
**Primary Key:** `uuid` (inherited from `BaseModel`)  
Records a single gym visit for a Member. Created on check-in; `check_out` and GPS coordinates are set on check-out.

| Field                       | Type              | Notes                                 |
| --------------------------- | ----------------- | ------------------------------------- |
| `uuid`                      | UUIDField         | PK, auto-generated                    |
| `member`                    | FK → CustomUser   | Must be `user_type=MEMBER`            |
| `check_in`                  | DateTimeField     | Required; set at check-in time        |
| `check_out`                 | DateTimeField     | Nullable; set at check-out time       |
| `check_in_lat`              | DecimalField(9,6) | GPS latitude at check-in (nullable)   |
| `check_in_lng`              | DecimalField(9,6) | GPS longitude at check-in (nullable)  |
| `check_out_lat`             | DecimalField(9,6) | GPS latitude at check-out (nullable)  |
| `check_out_lng`             | DecimalField(9,6) | GPS longitude at check-out (nullable) |
| `created_by` / `updated_by` | FK → User         | Audit (from BaseModel)                |
| `created_at` / `updated_at` | DateTimeField     | Auto-managed                          |
| `is_deleted` / `deleted_at` | —                 | Soft-delete (from BaseModel)          |

**Indexes:** Composite index `(member, check_in)` for inactivity queries.

---

### Enumerations

```python
class UserType(TextChoices):
    ADMIN = "ADMIN"
    GYM_OWNER = "GYM_OWNER"
    TRAINER = "TRAINER"
    MEMBER = "MEMBER"

class UserStatus(TextChoices):
    ACTIVE = "ACTIVE"
    DISABLED = "DISABLED"
    SUSPENDED = "SUSPENDED"
    DELETED = "DELETED"

class GenderChoice(TextChoices):
    MALE = "MALE"
    FEMALE = "FEMALE"
    OTHER = "OTHER"

class MembershipStatus(TextChoices):
    ACTIVE = "ACTIVE"
    EXPIRED = "EXPIRED"

class PaymentMode(TextChoices):
    CASH = "CASH"
    ONLINE = "ONLINE"
```

### BaseModel (abstract — `core/models.py`)

All domain models (`Membership`, `Payment`, and future workout/analytics models) inherit from `BaseModel`:

| Field        | Type                 | Notes                      |
| ------------ | -------------------- | -------------------------- |
| `uuid`       | UUIDField            | PK, auto UUID4             |
| `created_at` | DateTimeField        | Auto, indexed              |
| `updated_at` | DateTimeField        | Auto                       |
| `created_by` | FK → AUTH_USER_MODEL | Audit                      |
| `updated_by` | FK → AUTH_USER_MODEL | Audit                      |
| `is_deleted` | BooleanField         | Soft-delete flag           |
| `deleted_at` | DateTimeField        | Timestamp of soft deletion |

`BaseModel` exposes two managers:

- `objects` — default, returns all records including soft-deleted
- `active_objects` — `SoftDeleteManager`, filters out `is_deleted=True`

---

## Authentication

### Strategy

- **No public registration.** Accounts are created by higher-privileged roles only.
- Login uses `phone_number` + `password`.
- JWT is issued on successful login; all other endpoints require `Bearer <token>` in the `Authorization` header.
- JWT payload carries `user_type` and `status` as custom claims.

### JWT Configuration

| Setting                  | Value                                                               |
| ------------------------ | ------------------------------------------------------------------- |
| Access token lifetime    | 1440 min (1 day) — configurable via `ACCESS_TOKEN_TIME` env var     |
| Refresh token lifetime   | 43200 min (30 days) — configurable via `REFRESH_TOKEN_TIME` env var |
| Algorithm                | HS256                                                               |
| Signing key              | `JWT_SECRET_KEY` env var                                            |
| User ID field            | `uuid` (not `id`)                                                   |
| User ID claim            | `user_id`                                                           |
| Rotate refresh tokens    | Yes                                                                 |
| Blacklist after rotation | Yes                                                                 |
| Update last login        | Yes                                                                 |
| Custom obtain serializer | `accounts.serializers.LoginSerializer`                              |

### Account Status Enforcement on Login

Before issuing tokens, `LoginSerializer` pre-checks account status and raises HTTP 403 for:

- `DELETED` accounts
- `DISABLED` accounts
- `SUSPENDED` accounts

Invalid credentials raise HTTP 401.

### Token Blacklist

On logout the refresh token is explicitly blacklisted via `RefreshToken.blacklist()`. Token rotation also blacklists the old refresh token automatically.

---

## Password Policy

Enforced by `accounts.validators.StrongPasswordValidator` (registered in `AUTH_PASSWORD_VALIDATORS`) and mirrored inline in serializers via `_validate_password_strength()`:

- Minimum 8 characters
- At least one uppercase letter
- At least one lowercase letter
- At least one digit
- At least one special character from `!@#$%^&*(),.?":{}|<>-_=+[]\;'\`~/`

---

## API Endpoints

### Base URL Pattern

All responses are wrapped in the `ResponseRenderer` envelope:

```json
{
  "data": <payload or null>,
  "message": "<string>",
  "status": <http_status_code>,
  "time": "<ISO datetime>"
}
```

For paginated list responses the envelope also includes `count`, `next`, `previous`.

---

### Auth Endpoints — `/auth/`

| Method | Path                     | View                  | Auth Required | Description                                                                |
| ------ | ------------------------ | --------------------- | ------------- | -------------------------------------------------------------------------- |
| POST   | `/auth/login/`           | `LoginView`           | No            | Login with phone + password; returns access/refresh tokens + user snapshot |
| POST   | `/auth/token/refresh/`   | `TokenRefreshAPIView` | No            | Rotate refresh token; returns new access + refresh pair                    |
| POST   | `/auth/logout/`          | `LogoutView`          | Yes           | Blacklist the submitted refresh token                                      |
| GET    | `/auth/me/`              | `MeView`              | Yes           | Get full profile of the currently authenticated user                       |
| POST   | `/auth/change-password/` | `ChangePasswordView`  | Yes           | Change own password (old + new required; new must differ from old)         |

**Login response `data` shape:**

```json
{
  "access": "<jwt>",
  "refresh": "<jwt>",
  "user": {
    "uuid": "...",
    "first_name": "...",
    "last_name": "...",
    "phone_number": "...",
    "user_type": "ADMIN",
    "status": "ACTIVE",
    "gym_id": null,
    "trainer_id": null
  }
}
```

---

### Admin — Gym Owner Management — `/users/gym-owners/`

**Permission:** `IsAdmin` only

| Method | Path                                | Description                                                    |
| ------ | ----------------------------------- | -------------------------------------------------------------- |
| GET    | `/users/gym-owners/`                | List all active Gym Owners (paginated, filterable, searchable) |
| POST   | `/users/gym-owners/`                | Create a new Gym Owner                                         |
| GET    | `/users/gym-owners/{uuid}/`         | Retrieve a specific Gym Owner                                  |
| PATCH  | `/users/gym-owners/{uuid}/`         | Update a Gym Owner's profile                                   |
| DELETE | `/users/gym-owners/{uuid}/`         | Soft-delete a Gym Owner                                        |
| PATCH  | `/users/gym-owners/{uuid}/disable/` | Set Gym Owner status to DISABLED                               |

**Filters:** `status`  
**Search:** `first_name`, `last_name`, `phone_number`  
**Ordering:** `first_name`, `last_name`, `created_at` (default: `-created_at`)

---

### Gym Owner — Trainer Management — `/users/trainers/`

**Permission:** `IsGymOwner` only  
All queries are automatically scoped to the requesting Gym Owner's gym.

| Method | Path                              | Description                                                                             |
| ------ | --------------------------------- | --------------------------------------------------------------------------------------- |
| GET    | `/users/trainers/`                | List trainers in own gym                                                                |
| POST   | `/users/trainers/`                | Create a new Trainer (auto-assigned to own gym); rejected if `trainer_limit` is reached |
| GET    | `/users/trainers/{uuid}/`         | Retrieve a specific Trainer                                                             |
| PUT    | `/users/trainers/{uuid}/`         | Update a Trainer — name, status, and optional password change                           |
| DELETE | `/users/trainers/{uuid}/`         | Soft-delete a Trainer                                                                   |
| PATCH  | `/users/trainers/{uuid}/disable/` | Set Trainer status to DISABLED                                                          |

**Filters:** `status` **Search:** `first_name`, `last_name`, `phone_number`  
**Trainer Limit** — if `gym_owner.trainer_limit > 0` and the count of active trainers has reached the limit, creation returns HTTP 400.

---

### Gym Owner — Member Management — `/users/members/`

**Permission:** `IsGymOwner` only  
All queries are automatically scoped to the requesting Gym Owner's gym.

| Method | Path                                    | Description                                                                                           |
| ------ | --------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| GET    | `/users/members/`                       | List members in own gym                                                                               |
| POST   | `/users/members/`                       | Create a new Member (auto-assigned to own gym); optional `trainer_uuid` assigns a trainer immediately |
| GET    | `/users/members/{uuid}/`                | Retrieve a specific Member                                                                            |
| PUT    | `/users/members/{uuid}/`                | Update a Member — name, status, `trainer_id` (reassign/unassign), optional password                   |
| DELETE | `/users/members/{uuid}/`                | Soft-delete a Member                                                                                  |
| PATCH  | `/users/members/{uuid}/disable/`        | Set Member status to DISABLED                                                                         |
| PATCH  | `/users/members/{uuid}/assign-trainer/` | Assign (or reassign) a Trainer to a Member                                                            |

**Filters:** `status`, `gender`, `trainer`  
**Trainer reassignment via PUT** — pass `trainer_id: "<uuid>"` to reassign, `trainer_id: null` to unassign. Trainer must belong to the same gym; cross-gym assignment is rejected with HTTP 400.

---

### Gym Owner / Admin — Membership Management — `/memberships/`

**Permission:** `IsAdminOrGymOwner`  
Gym Owners see only memberships for members of their own gym. Admins see all.

| Method | Path                   | Description                                                              |
| ------ | ---------------------- | ------------------------------------------------------------------------ |
| GET    | `/memberships/`        | List memberships; filter by `?status=`, `?member=`, `?payment_mode=`     |
| POST   | `/memberships/`        | Create a membership for a member; validates non-overlapping active dates |
| GET    | `/memberships/{uuid}/` | Retrieve a membership                                                    |
| PUT    | `/memberships/{uuid}/` | Update a membership (extend dates, correct amount/mode)                  |
| DELETE | `/memberships/{uuid}/` | Soft-delete / expire a membership                                        |

**Business rules enforced:**

- `start_date` must be ≤ `end_date` (400 otherwise)
- A member cannot have two `ACTIVE` memberships with overlapping date ranges (400)
- `member` must be a user of type `MEMBER`
- `status` defaults to `ACTIVE` on creation

---

### Gym Owner / Admin — Payment Management — `/payments/`

**Permission:** `IsAdminOrGymOwner`  
Gym Owners see only payments for their members. Admins see all.

| Method | Path                | Description                                                                |
| ------ | ------------------- | -------------------------------------------------------------------------- |
| GET    | `/payments/`        | List payments; use `?member=<uuid>` to filter by member across memberships |
| POST   | `/payments/`        | Record a payment against an existing membership                            |
| GET    | `/payments/{uuid}/` | Retrieve a payment                                                         |
| PUT    | `/payments/{uuid}/` | Correct a payment entry                                                    |
| DELETE | `/payments/{uuid}/` | Soft-delete a payment record                                               |

**Business rules enforced:**

- `amount` must be > 0 (400 otherwise)
- `membership` must belong to a member in the requesting Gym Owner's gym (400 otherwise)
- `paid_on` defaults to current timestamp if not provided

---

### Trainer Panel — `/trainer/members/`

**Permission:** `IsTrainer` only  
Read-only. Only returns Members assigned to the requesting Trainer.

| Method | Path                       | Description                                       |
| ------ | -------------------------- | ------------------------------------------------- |
| GET    | `/trainer/members/`        | List own assigned Members (paginated, searchable) |
| GET    | `/trainer/members/{uuid}/` | Retrieve a specific assigned Member               |

**HTTP methods allowed:** GET, HEAD, OPTIONS only.

---

### Member Panel — `/member/profile/`

**Permission:** `IsMember` only

| Method | Path               | Description                                                                                              |
| ------ | ------------------ | -------------------------------------------------------------------------------------------------------- |
| GET    | `/member/profile/` | Get own profile                                                                                          |
| PATCH  | `/member/profile/` | Update own profile (first_name, last_name, profile_picture, gender only — phone, DOB, age are read-only) |

---

### Utility Endpoints — `/api/`

| Method | Path           | Auth Required | Description                                                 |
| ------ | -------------- | ------------- | ----------------------------------------------------------- |
| HEAD   | `/api/health/` | No            | Health check — returns HTTP 200                             |
| GET    | `/api/ping/`   | No            | Returns `"pong"`                                            |
| GET    | `/api/my-ip/`  | No            | Returns client IP, GeoIP location, browser, OS, device info |

---

### Attendance — `/api/attendance/`

**Permission:** `IsGymOwner | IsTrainer`  
Gym Owners see attendance for all members in their gym. Trainers see attendance for their assigned members only.

| Method | Path                        | Description                                                                    |
| ------ | --------------------------- | ------------------------------------------------------------------------------ |
| POST   | `/api/attendance/checkin/`  | Record a member check-in (`member_id`, `timestamp`, optional `lat`/`lng`)      |
| POST   | `/api/attendance/checkout/` | Record a member check-out (`attendance_id`, `timestamp`, optional `lat`/`lng`) |
| GET    | `/api/attendance/`          | List attendance records; filter by `?member_id=` and/or `?date=YYYY-MM-DD`     |

**Check-in rules:** Member must belong to the requesting Gym Owner's gym, or be assigned to the requesting Trainer.  
**Check-out rules:** `attendance_id` must reference an open (non-checked-out) record; returns HTTP 400 if already checked out.

---

### Reports — `/api/reports/`

**Permission:** `IsGymOwner` only  
All queries are scoped to the requesting Gym Owner's gym.

| Method | Path                              | Query Param           | Description                                                                                              |
| ------ | --------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------- |
| GET    | `/api/reports/inactive-members/`  | `?days=N` (default 7) | Members with no check-in in the last N days; returns `member_id`, `name`, `last_visit`, `days_inactive`  |
| GET    | `/api/reports/trainer-workload/`  | —                     | Each trainer with their active member count; returns `trainer_id`, `name`, `member_count`                |
| GET    | `/api/reports/membership-expiry/` | `?days=X` (default 7) | Members whose `membership_end ≤ today + X days`; returns `member_id`, `name`, `expiry_date`, `days_left` |

---

### Phase-3 Member Payments — `/api/payments/`

**Permission:** `IsAdminOrGymOwner`  
A single-step endpoint that records a payment **and** updates the member's membership snapshot fields (`membership_start`, `membership_end`, `membership_status`) in one atomic operation. Internally creates a `Membership` record.

| Method | Path             | Description                                                              |
| ------ | ---------------- | ------------------------------------------------------------------------ |
| POST   | `/api/payments/` | Record a membership payment; update member's membership dates and status |
| GET    | `/api/payments/` | List memberships for the gym; filter by `?member_id=`                    |

**POST body:**

```json
{
  "member_id": "<uuid>",
  "date": "2026-06-01",
  "amount": "1500.00",
  "mode": "Cash",
  "start_date": "2026-06-01",
  "end_date": "2026-06-30",
  "plan": "Monthly"
}
```

> **Note:** The legacy `/payments/` endpoint (Phase 2) remains at the root level and records individual payment transactions against an existing `Membership` FK. `/api/payments/` is the simpler Phase-3 interface.

---

### Backup / Sync — `/api/backup/`

**Permission:** `IsAdmin | IsGymOwner | IsTrainer`  
Offline-first sync endpoints. The server acts as a cloud backup; conflict resolution uses Last-Write-Wins (LWW) on `updated_at`.

| Method | Path                    | Description                                                                                       |
| ------ | ----------------------- | ------------------------------------------------------------------------------------------------- |
| POST   | `/api/backup/upload/`   | Push client-side changes to the server (supports `create`, `update`, `delete` actions per record) |
| GET    | `/api/backup/download/` | Pull server-side records changed since a timestamp (`?user_id=&since=<ISO datetime>`)             |

**Upload payload:**

```json
{
  "user_id": "<member uuid>",
  "changes": [
    {"model": "attendance.Attendance", "action": "create", "data": {...}},
    {"model": "attendance.Attendance", "action": "update", "data": {...}},
    {"model": "attendance.Attendance", "action": "delete", "data": {"uuid": "..."}}
  ]
}
```

**LWW rule:** On `update`, if the server record's `updated_at ≥` client's `updated_at`, the change is skipped (server wins). The response summarises `created`, `updated`, `deleted`, `skipped`, `errors` counts.  
**Supported models:** `attendance.Attendance` (Phase 3). Extend `_SYNCABLE` dict in `backup/views.py` for Phase 4 workout models.

---

### API Documentation

| Path       | Description                          |
| ---------- | ------------------------------------ |
| `/schema/` | OpenAPI 3 schema (JSON/YAML)         |
| `/docs/`   | Swagger UI interactive documentation |

---

## Permission Classes

Defined in `core/permissions.py`:

| Class                 | Condition                                       |
| --------------------- | ----------------------------------------------- |
| `IsAdmin`             | `user_type == ADMIN`                            |
| `IsGymOwner`          | `user_type == GYM_OWNER`                        |
| `IsTrainer`           | `user_type == TRAINER`                          |
| `IsMember`            | `user_type == MEMBER`                           |
| `IsAdminOrGymOwner`   | `user_type in (ADMIN, GYM_OWNER)`               |
| `IsAuthenticatedUser` | Extends `IsAuthenticated` with a custom message |
| `HavePermissions`     | Extends `DjangoModelPermissions`                |

All custom permission classes check `is_authenticated` first and return a descriptive `message` on failure.

---

## Serializers

### Auth Serializers (`accounts/serializers.py`)

| Serializer                 | Purpose                                                                                                                   |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `LoginSerializer`          | Extends `TokenObtainPairSerializer`; adds `user_type`/`status` claims to JWT; returns user snapshot alongside tokens      |
| `LogoutSerializer`         | Accepts `refresh` token string                                                                                            |
| `ChangePasswordSerializer` | Validates `old_password` against stored hash; enforces strong password on `new_password`; prevents reuse of same password |
| `UserMeSerializer`         | Read-only snapshot of authenticated user (all profile fields + computed `age`)                                            |

### Gym Owner Serializers

| Serializer                 | Used For                                                                             |
| -------------------------- | ------------------------------------------------------------------------------------ |
| `GymOwnerCreateSerializer` | POST — requires phone, password, first_name, last_name, gender                       |
| `GymOwnerDetailSerializer` | GET/PATCH — includes `age` computed field; phone_number/uuid/user_type are read-only |

### Trainer Serializers

| Serializer                | Used For                                                                                                          |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `TrainerCreateSerializer` | POST — requires phone_number, first_name, last_name, gender, password                                             |
| `TrainerDetailSerializer` | GET/PUT — includes `age`, `gym_id` (read-only); optional `password` field for updates (hashed via `set_password`) |

### Member Serializers

| Serializer                | Used For                                                                                                                          |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `MemberCreateSerializer`  | POST — accepts optional `trainer_uuid` (validated against gym scope)                                                              |
| `MemberDetailSerializer`  | GET/PUT — includes `age`, `gym_id` (read-only); `trainer_id` is writable (gym-scoped validation); optional `password` for updates |
| `MemberProfileSerializer` | Member self-edit — phone_number, date_of_birth, age are read-only                                                                 |
| `AssignTrainerSerializer` | PATCH `/assign-trainer/` — accepts `trainer_uuid`                                                                                 |

### Membership Serializers

| Serializer             | Used For                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `MembershipSerializer` | GET / POST / PUT on `/memberships/` — validates `start_date ≤ end_date`; enforces no overlapping active memberships on create; defaults `status` to `ACTIVE` |

### Payment Serializers

| Serializer          | Used For                                                                                                   |
| ------------------- | ---------------------------------------------------------------------------------------------------------- |
| `PaymentSerializer` | GET / POST / PUT on `/payments/` — validates `amount > 0`; validates membership belongs to requester's gym |

### Phase-3 Payment Serializers

| Serializer                        | Used For                                                                                                                                     |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `MemberPaymentSerializer`         | Input for `POST /api/payments/` — accepts `member_id`, `date`, `amount`, `mode` (`Cash`/`Online`), `start_date`, `end_date`, optional `plan` |
| `MemberPaymentResponseSerializer` | Output shape for `POST /api/payments/` — surfaces `uuid`, `member_id`, `amount`, `date`, `start_date`, `end_date`, `mode`, `plan`, `status`  |

### Attendance Serializers (`attendance/serializers.py`)

| Serializer             | Used For                                                                                        |
| ---------------------- | ----------------------------------------------------------------------------------------------- |
| `AttendanceSerializer` | Response shape for all attendance views — full record including GPS fields                      |
| `CheckInSerializer`    | Input for `POST /api/attendance/checkin/` — `member_id`, `timestamp`, optional `lat`/`lng`      |
| `CheckOutSerializer`   | Input for `POST /api/attendance/checkout/` — `attendance_id`, `timestamp`, optional `lat`/`lng` |

All create serializers call `instance.full_clean()` (via `_run_model_validation()`) before saving, so model-level relationship constraints are enforced at the API layer.

---

## Managers

**`UserManager`** (in `accounts/managers.py`):

- `create_user(phone_number, password, **extra_fields)` — standard user creation
- `create_superuser(phone_number, password, **extra_fields)` — forces `is_staff=True`, `is_superuser=True`, `user_type=ADMIN`, `status=ACTIVE`

**`ActiveUserManager`** — filters out soft-deleted users (`is_deleted=False`).  
Exposed on `CustomUser` as `CustomUser.active_objects`.

---

## Soft Delete

All user deletions via the API are **soft deletes** — no records are physically removed.

`soft_delete(deleted_by=None)` sets:

- `is_deleted = True`
- `status = DELETED`
- `deleted_at = now()`
- `updated_by = deleted_by` (if provided)

The `ActiveUserManager` (`active_objects`) automatically excludes soft-deleted records. All view querysets use `active_objects`, so deleted users are invisible to the API.

---

## Pagination

`core.pagination.CustomPagination` (extends `PageNumberPagination`):

| Parameter               | Default     | Max |
| ----------------------- | ----------- | --- |
| `page_size`             | 20          | 100 |
| `page_size_query_param` | `page_size` | —   |
| `page_query_param`      | `page`      | —   |

---

## Response Envelope (`ResponseRenderer`)

Every API response is normalized through `core.renderers.ResponseRenderer`:

```json
{
  "data": <payload>,
  "message": "<string>",
  "status": <http_code>,
  "time": "<datetime>"
}
```

- On **2xx**: `data` contains the payload; `message` is the `detail` field if present; for paginated responses `count`, `next`, `previous` are hoisted to the top level.
- On **non-2xx**: `data` is set to `null`; `message` is populated from `detail`.

---

## Custom Exceptions

`core.exceptions.ConflictException` — raised when a phone number is already registered:

- HTTP status: `409 Conflict`
- Default code: `conflict`

---

## Logging

Log files are auto-created in `logs/` at startup:

| File                    | Content                              |
| ----------------------- | ------------------------------------ |
| `logs/logs.log`         | General Django INFO and DEBUG output |
| `logs/sql.log`          | SQL query log                        |
| `logs/errors.log`       | Application-level errors             |
| `logs/system-error.log` | ERROR-level system errors            |

Format: `{levelname} {asctime} {module} {process:d} {thread:d} {message}`

---

## Email

SMTP email is configured for transactional email delivery (e.g., future notification features):

- Backend: `smtp.EmailBackend`
- Host: `smtp.gmail.com` (configurable via env)
- Port: 587, TLS enabled
- Credentials from env vars: `EMAIL_HOST_USER`, `EMAIL_HOST_PASSWORD`
- App branding env vars: `APP_NAME`, `LOGO_URL`, `SUPPORT_EMAIL`

`core.utils.mail_letter_sender(subject, to_email, template, context)` — utility for sending HTML emails from Django templates.

---

## Management Commands

### `create_admin`

Bootstrap the first Platform Admin user. Run once during initial deployment.

```bash
python manage.py create_admin \
  --phone 9000000001 \
  --password "Admin@1234" \
  --first-name "Platform" \
  --last-name "Admin"
```

Raises `CommandError` if the phone number already exists. Sets `is_staff=True`, `is_superuser=True`, `user_type=ADMIN`.

---

## Django Admin

`accounts/admin.py` registers three models:

**`CustomUserAdmin`** — full-featured panel for `CustomUser`:

- **List display:** phone_number, first/last name, user_type, status, gym, trainer, is_active, created_at
- **Filters:** user_type, status, is_active, is_staff, gender
- **Search:** phone_number, first_name, last_name
- **Read-only fields:** uuid, created_at, updated_at, created_by, updated_by, deleted_at, profile_picture_preview, age
- **Profile picture preview:** rendered as a circular thumbnail in the admin detail view
- Separate `add_form` and `change_form` for password handling

**`MembershipAdmin`**:

- **List display:** uuid, member, plan, start_date, end_date, amount_paid, payment_mode, status, created_at
- **Filters:** status, payment_mode
- **Search:** member phone/name, plan

**`PaymentAdmin`**:

- **List display:** uuid, membership, amount, mode, paid_on, created_at
- **Filters:** mode
- **Search:** membership member phone/name

`attendance/admin.py` registers one model:

**`AttendanceAdmin`**:

- **List display:** member, check_in, check_out, is_deleted
- **Filters:** is_deleted
- **Search:** member first_name, last_name, phone_number
- **Raw ID fields:** member

---

## Environment Variables

Copy `.env.example` to `.env` and fill in all values before running the server.

| Variable                    | Description                                              |
| --------------------------- | -------------------------------------------------------- |
| `SECRET_KEY`                | Django secret key                                        |
| `JWT_SECRET_KEY`            | Separate signing key for JWT tokens                      |
| `ACCESS_TOKEN_TIME`         | Access token lifetime in minutes (default: 1440)         |
| `REFRESH_TOKEN_TIME`        | Refresh token lifetime in minutes (default: 43200)       |
| `DJANGO_ENV`                | `development` (DEBUG=True) or `production` (DEBUG=False) |
| `HOST`                      | Database host                                            |
| `DATABASE_NAME`             | MySQL database name                                      |
| `DATABASE_USER`             | MySQL user                                               |
| `DATABASE_PASS`             | MySQL password                                           |
| `DATABASE_PORT`             | MySQL port (default: 3306)                               |
| `EMAIL_HOST_USER`           | SMTP login email                                         |
| `EMAIL_HOST_PASSWORD`       | SMTP password                                            |
| `DEFAULT_FROM_EMAIL`        | From address for outgoing mail                           |
| `SUPPORT_EMAIL`             | Support email shown in templates                         |
| `APP_NAME`                  | App branding name (used in email templates)              |
| `LOGO_URL`                  | Logo URL (used in email templates)                       |
| `CONTACT_US_EMAIL_RECEIVER` | Destination for contact form submissions                 |

---

## Database

MySQL with strict mode (`STRICT_TRANS_TABLES`), UTF-8 charset, port 3306 by default.

All connection parameters are loaded from environment variables. No credentials are hardcoded.

---

## Tests

Full test suite in `accounts/tests.py` using Django's `TestCase` + DRF's `APIClient`.

### Test Classes

| Class                    | Coverage                                                                                                              |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| `AuthTests`              | Login (success, wrong creds, disabled, suspended), logout (success, bad token), `/me`, change-password, token refresh |
| `AdminTests`             | Admin creates Gym Owner, non-admin blocked, soft-delete, list                                                         |
| `TrainerManagementTests` | Create trainer, duplicate phone (409), disable, update, soft-delete, gym scoping (own gym only), member blocked       |
| `MemberManagementTests`  | Create member, assign trainer, duplicate phone (409), disable, update, soft-delete, cross-gym trainer rejection       |
| `TrainerPanelTests`      | Trainer sees only own assigned members                                                                                |
| `MemberProfileTests`     | Member reads and updates own profile, Gym Owner blocked from member panel                                             |

**Run tests:**

```bash
python manage.py test accounts
```

---

## Data Scoping Rules

| Actor     | Visible Data                                                                                                        |
| --------- | ------------------------------------------------------------------------------------------------------------------- |
| Admin     | All Gym Owners, Trainers, Members, Memberships, Payments, Attendance (across all gyms)                              |
| Gym Owner | Trainers and Members in their gym; Memberships, Payments, Attendance for their members; Reports scoped to their gym |
| Trainer   | Only Members assigned to them (`trainer=request.user`); Attendance for their assigned members                       |
| Member    | Only their own profile (no server endpoints for attendance/payments — all offline)                                  |

Cross-gym operations (e.g., assigning a trainer from a different gym) are rejected at the queryset level, not just in business logic — the foreign-gym trainer simply does not appear in the queryset, resulting in HTTP 404.

Membership/Payment scoping is enforced via queryset filters (`member__gym=request.user` and `membership__member__gym=request.user` respectively).

Attendance scoping: `member__gym=request.user` for Gym Owners; `member__trainer=request.user` for Trainers.

---

## Deployment Notes

- `ALLOWED_HOSTS = ["*"]` and `CORS_ALLOW_ALL_ORIGINS = True` — tighten for production
- In production (`DJANGO_ENV != development`), static and media files are served via Django's `serve` view — consider a proper web server (nginx) for production scale
- The `logs/` directory is auto-created by `settings.py` on startup
- The GeoLite2 City database must be placed at `static/geo_lite2/GeoLite2-City.mmdb` for the `/api/my-ip/` endpoint to work
- Run `python manage.py collectstatic` for static file collection before production deployment
- Run `python manage.py migrate` on first deploy and after each schema change
- Bootstrap the first admin with `python manage.py create_admin`

---

## Migrations

| Migration                                          | App          | File                                  | Changes                                                                                                                               |
| -------------------------------------------------- | ------------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `0001_initial`                                     | `accounts`   | `accounts/migrations/0001_initial.py` | Creates the `users` table for `CustomUser`                                                                                            |
| `0002_customuser_trainer_limit_membership_payment` | `accounts`   | `accounts/migrations/0002_...py`      | Adds `trainer_limit` to `users`; creates `memberships` and `payments` tables                                                          |
| `0003_customuser_membership_fields`                | `accounts`   | `accounts/migrations/0003_...py`      | Adds `membership_start`, `membership_end`, `membership_status`, `membership_plan` to `users`; adds composite index `(gym, user_type)` |
| `0001_initial` _(attendance)_                      | `attendance` | auto-generated by `makemigrations`    | Creates `attendance` table with GPS fields and composite index `(member, check_in)`                                                   |

> Run `python manage.py makemigrations attendance` then `python manage.py migrate` to apply all pending migrations.

---

## Phase Roadmap

### Phase 1 — Complete

User management and authentication system as documented above.

### Phase 2 — Complete

Gym-owner and trainer management features:

- `trainer_limit` field on `CustomUser` — enforced on trainer creation
- `Membership` model — tracks membership terms per member with date-overlap prevention
- `Payment` model — records individual payment transactions against memberships
- Full CRUD API for Memberships (`/memberships/`) and Payments (`/payments/`) with `IsAdminOrGymOwner` permission
- PUT support for password changes on Trainer and Member updates
- PUT support for trainer reassignment on Member updates
- Admin panel registration for Membership and Payment

### Phase 3 — Complete

Offline-first cloud backup, attendance tracking, reporting, and notification hooks:

- **CustomUser membership snapshot** — `membership_start`, `membership_end`, `membership_status`, `membership_plan` fields + composite `(gym, user_type)` DB index
- **`attendance` app** — `Attendance` model with GPS check-in/out; `POST /api/attendance/checkin/`, `POST /api/attendance/checkout/`, `GET /api/attendance/` endpoints; composite index `(member, check_in)`
- **`/api/payments/` (Phase-3)** — `MemberPaymentView` creates a `Membership` record **and** atomically updates the member's snapshot fields in one API call
- **`reports` app** — three read-only Gym Owner endpoints:
  - `GET /api/reports/inactive-members/?days=N`
  - `GET /api/reports/trainer-workload/`
  - `GET /api/reports/membership-expiry/?days=X`
- **`backup` app** — LWW offline sync:
  - `POST /api/backup/upload/` — push client changes (create/update/delete) with Last-Write-Wins conflict resolution
  - `GET /api/backup/download/?user_id=&since=` — pull server-side changes since a timestamp
- **`notifications` module** — `send_sms()` and `send_whatsapp()` placeholder functions in `notifications/services.py`; no external provider integrated yet
